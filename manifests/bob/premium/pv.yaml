apiVersion: v1
kind: PersistentVolume
metadata:
  name: bob-premium
spec:
  accessModes:
    - ReadWriteMany
  capacity:
    storage: 100Gi
  csi:
    driver: blob.csi.azure.com
    nodeStageSecretRef:
      name: azure-secret-premium
      namespace: default
    readOnly: false
    volumeAttributes:
      containerName: bob
    volumeHandle: bob-premium
  persistentVolumeReclaimPolicy: Retain
